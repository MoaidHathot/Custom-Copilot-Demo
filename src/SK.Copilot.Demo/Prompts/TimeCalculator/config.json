{
	"schema": 1,
	"type": "completion",
	"description": "Calculate the date and time based on the current time and the user request",
	"execution_settings": {
		"default": {
			"max_tokens": 4000,
			"temperature": 0,
			"top_p": 0.0,
			"presence_penalty": 0.0,
			"frequency_penalty": 0.0
		}
	},
	"input_variables": [
		{
			"name": "input",
			"description": "The user's input.",
			"required": true
		}
	]
}
